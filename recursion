// n! = n*(n-1) = factorial formula

#include<stdio.h> 

unsigned long long int  factorial( unsigned int i){  //4 //5*4*3*2
    if ( i <=1 ){
        return 1;
    }
    return i* factorial(i -1);  ///4*factorial(4-1)
}
 int main (){
     int i = 5;
     printf("%d",factorial(i));
 }
